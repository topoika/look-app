import 'package:flutter/cupertino.dart';
import 'package:look/base/models/settings_model.dart';

ValueNotifier<Settings> settings = ValueNotifier(Settings());

void setDeviceSettins() {
  //TODO
  //ADD sharedpreferences to store the user settings on the app cache
}
